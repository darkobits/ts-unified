{
  "compilerOptions": {
    // Specify a list of built-in API groups to include in the project.
    "lib": [
      "esnext",
      "es2015"
    ],

    // Output code will be optimized for this language spec. ES1028 is fully
    // supported in Node 10.
    "target": "es2018",

    // Generate output code that uses CommonJS (ie: require).
    "module": "commonjs",

    // Transform absolute imports in source files to relative imports so that
    // output code works as expected.
    "plugins": [
      {"transform": "ts-transformer-imports"},
      {"transform": "ts-transformer-imports", "afterDeclarations": true}
    ],

    // Allow default imports from modules with no default export. This does not
    // affect code emit, just typechecking.
    "allowSyntheticDefaultImports": true,

    // Parse in strict mode.
    "alwaysStrict": true,

    // Emit declaration files.
    "declaration": true,

    // Emit design-type metadata for decorated declarations in source.
    "emitDecoratorMetadata": true,

    // This allows us to use synthetic default imports in emitted code.
    "esModuleInterop": true,

    // Enables experimental support for ES7 decorators.
    "experimentalDecorators": true,

    // Warn on expressions and declarations with an implied 'any' type.
    "noImplicitAny": true,

    // Report errors on unused locals.
    "noUnusedLocals": true,

    // Stylize errors and messages using color and context.
    "pretty": true,

    // Enable strict null checks.
    "strictNullChecks": true
  }
}
