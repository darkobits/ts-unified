// tslint:disable no-useless-files

/**
 * This file is required or TypeScript will complain that it can't find anything
 * to compile. TypeScript is still involved in this project's build command
 * despite it not having any TypeScript files in to keep things as consistent as
 * possible, but this tiny kludge is needed to make things work.
 */
